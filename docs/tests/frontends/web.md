# Web Test Matrix

This matrix records current frontend test coverage shape.

| Area | Automated evidence | Gap |
|---|---|---|
| Type contract | The live gateway OpenAPI aggregate and orval client were regenerated after adding inventory documents, ledger endpoints and `VACCINE`; `pnpm --filter @petclinic/web typecheck` passes. | None for the current gateway contract. |
| Static/production build | ESLint and Vite production build pass. | Bundle-size warnings remain for existing large admin/workflow chunks. |
| Unit/component | `inventory-metrics.test.ts` verifies dashboard totals. `stock-document.test.ts` verifies multi-line documents and duplicate-product rejection. `navigation.test.ts` locks the STAFF menu to reception/cashier routes, and `public-header.test.tsx` verifies role-specific portal destinations. | Add component tests for add/remove-line interactions and ledger error/empty states. |
| Public landing/header | `src/components/public-header.test.tsx` verifies complete shared navigation, hotline, mobile-menu accessibility, and role portal routing. `src/features/landing/components/LandingImage.test.tsx` verifies local image fallback behavior. A source scan guards the landing against remote image URLs and legacy `MS501` branding. | Visual-regression snapshots for desktop and mobile are not automated yet. |
| Store catalog | `src/features/store/components/StorePage.test.tsx` verifies the in-store-only notice, guards against cart/delivery/online-payment actions, checks dynamic categories, asserts unique image sources for all 19 current product codes, and covers image-load fallback behavior. | Full catalog API behavior remains to be covered in a browser flow. |
| Browser e2e | All four Chromium tests pass. `smoke.spec.ts` covers landing, login accessibility and password visibility. `clinical-journey.spec.ts` uses `/staff/invoices` for cashier checkout and `/inventory/products` for ledger reconciliation. `inventory-flow.spec.ts` exercises the dedicated `/inventory` portal, its dashboard/category navigation, a two-line receipt, a two-line issue, the 50-row consolidated ledger response, all four ledger entries and recent dashboard activity. | Admin dashboard widgets and workflow-designer browser flows remain uncovered. |
| Customer rating state | `customer-rating-state.test.ts` verifies paginated persistent lookup; `CustomerVisitRow.test.tsx` verifies rated visits expose no actionable rating button. | Detail-dialog component behavior is covered by the browser journey rather than a dedicated component test. |
| AI assistant transport | `pnpm --filter @petclinic/web typecheck` verifies the `DefaultChatTransport` / UIMessage tool-part contract and all role portal mounts compile. | A live browser turn still needs a configured LLM and MCP server; no provider-dependent e2e is recorded. |
| Invoice screen scope | The shared invoice editor exposes treatment and retail sections independently; the vet dialog hides retail and the cashier screen hides treatment. The cross-role Playwright journey verifies automatic reconciliation of asynchronously arriving medication without a manual refresh, both operational screens, and the customer's paid-invoice line items and linked clinical record. | Add focused component coverage for cancelled invoices, missing visit records and multiple visits consolidated into one invoice. |

Use `docs/guidelines/testing-harness.md` for commands.

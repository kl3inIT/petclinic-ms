databaseChangeLog:

  # Dev-only customer account for customer portal smoke testing.
  #
  # Password: "customer123" (BCrypt 12 rounds).
  # customer_id intentionally NULL: the dev owners seed does not reserve a stable
  # one-to-one auth.users.customer_id mapping yet.

  - changeSet:
      id: 008-001-seed-dev-customer-user
      author: mss301
      context: dev
      changes:
        - sql:
            sql: >-
              INSERT INTO auth.users (id, username, email, password, roles_csv, enabled, vet_id, customer_id) VALUES
                ('11111111-1111-1111-1111-000000000008', 'customer1', 'customer1@petclinic.local', '$2a$12$ZETRbc6CCKI/7fEKbESm.Oh0r7TNfI3XtHyr7q83iYXTn9yVMDZ7K', 'USER', TRUE, NULL, NULL)
              ON CONFLICT (username) DO NOTHING

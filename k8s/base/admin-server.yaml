# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-server
  labels: { app: admin-server, tier: platform }
spec:
  replicas: 1
  selector: { matchLabels: { app: admin-server } }
  template:
    metadata:
      labels: { app: admin-server }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9185"
        prometheus.io/path: /actuator/prometheus
    spec:
      containers:
        - name: admin-server
          image: petclinic-ms/admin-server:latest
          ports:
            - { name: http, containerPort: 8185 }
            - { name: mgmt, containerPort: 9185 }
          envFrom:
            - configMapRef: { name: petclinic-common }
          env:
            - { name: ADMIN_USER, value: "admin" }
            - name: ADMIN_PASSWORD
              valueFrom: { secretKeyRef: { name: petclinic-secrets, key: ADMIN_PASSWORD } }
          resources:
            requests: { cpu: 200m, memory: 256Mi }
            limits: { cpu: "1", memory: 512Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9185 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9185 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9185 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: admin-server
  labels: { app: admin-server }
spec:
  selector: { app: admin-server }
  ports:
    - { name: http, port: 8185, targetPort: 8185 }
    - { name: mgmt, port: 9185, targetPort: 9185 }

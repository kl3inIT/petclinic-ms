# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reviews-service
  labels: { app: reviews-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: reviews-service } }
  template:
    metadata:
      labels: { app: reviews-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9189"
        prometheus.io/path: /actuator/prometheus
    spec:
      containers:
        - name: reviews-service
          image: petclinic-ms/reviews-service:latest
          ports:
            - { name: http, containerPort: 8189 }
            - { name: mgmt, containerPort: 9189 }
          envFrom:
            - configMapRef: { name: petclinic-common }
          resources:
            requests: { cpu: 200m, memory: 384Mi }
            limits: { cpu: "1", memory: 768Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9189 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9189 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9189 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: reviews-service
  labels: { app: reviews-service }
spec:
  selector: { app: reviews-service }
  ports:
    - { name: http, port: 8189, targetPort: 8189 }
    - { name: mgmt, port: 9189, targetPort: 9189 }

# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-service
  labels: { app: products-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: products-service } }
  template:
    metadata:
      labels: { app: products-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9192"
        prometheus.io/path: /actuator/prometheus
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: products-service
          image: petclinic-ms/products-service:latest
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
          ports:
            - { name: http, containerPort: 8192 }
            - { name: mgmt, containerPort: 9192 }
          envFrom:
            - configMapRef: { name: petclinic-common }
          resources:
            requests: { cpu: 200m, memory: 384Mi }
            limits: { cpu: "1", memory: 768Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9192 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9192 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9192 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: products-service
  labels: { app: products-service }
spec:
  selector: { app: products-service }
  ports:
    - { name: http, port: 8192, targetPort: 8192 }
    - { name: mgmt, port: 9192, targetPort: 9192 }

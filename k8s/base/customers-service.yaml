# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customers-service
  labels: { app: customers-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: customers-service } }
  template:
    metadata:
      labels: { app: customers-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9181"
        prometheus.io/path: /actuator/prometheus
    spec:
      containers:
        - name: customers-service
          image: petclinic-ms/customers-service:latest
          ports:
            - { name: http, containerPort: 8181 }
            - { name: mgmt, containerPort: 9181 }
          envFrom:
            - configMapRef: { name: petclinic-common }
          resources:
            requests: { cpu: 200m, memory: 384Mi }
            limits: { cpu: "1", memory: 768Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9181 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9181 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9181 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: customers-service
  labels: { app: customers-service }
spec:
  selector: { app: customers-service }
  ports:
    - { name: http, port: 8181, targetPort: 8181 }
    - { name: mgmt, port: 9181, targetPort: 9181 }

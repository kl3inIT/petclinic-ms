# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mailer-service
  labels: { app: mailer-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: mailer-service } }
  template:
    metadata:
      labels: { app: mailer-service }
    spec:
      containers:
        - name: mailer-service
          image: petclinic-ms/mailer-service:latest
          ports:
            - { name: http, containerPort: 8186 }
          env:
            - { name: AMQP_URL, value: "amqp://guest:guest@rabbitmq:5672/" }
            - { name: REDIS_ADDR, value: "redis:6379" }
            - { name: SMTP_HOST, value: "smtp" }
            - { name: SMTP_PORT, value: "1025" }
            - { name: ADMIN_SERVER_URL, value: "http://admin-server:8185" }
            - { name: ADMIN_PUBLIC_HOST, value: "mailer-service" }
            - { name: ADMIN_USER, value: "admin" }
            - name: ADMIN_PASSWORD
              valueFrom: { secretKeyRef: { name: petclinic-secrets, key: ADMIN_PASSWORD } }
          resources:
            requests: { cpu: 50m, memory: 64Mi }
            limits: { cpu: 250m, memory: 128Mi }
          readinessProbe:
            httpGet: { path: /health, port: 8186 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8186 }
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: mailer-service
  labels: { app: mailer-service }
spec:
  selector: { app: mailer-service }
  ports:
    - { name: http, port: 8186, targetPort: 8186 }

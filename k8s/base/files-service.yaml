# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: files-service
  labels: { app: files-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: files-service } }
  template:
    metadata:
      labels: { app: files-service }
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: files-service
          image: petclinic-ms/files-service:latest
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
          ports:
            - { name: http, containerPort: 8193 }
          env:
            - { name: MINIO_ENDPOINT, value: "http://minio:9000" }
            - { name: MINIO_BUCKET_PETS, value: "pet-photos" }
            - { name: MINIO_BUCKET_INVOICES, value: "invoices" }
            - name: MINIO_ACCESS_KEY
              valueFrom: { secretKeyRef: { name: petclinic-secrets, key: MINIO_ACCESS_KEY } }
            - name: MINIO_SECRET_KEY
              valueFrom: { secretKeyRef: { name: petclinic-secrets, key: MINIO_SECRET_KEY } }
          resources:
            requests: { cpu: 50m, memory: 64Mi }
            limits: { cpu: 250m, memory: 128Mi }
          readinessProbe:
            httpGet: { path: /health, port: 8193 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8193 }
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: files-service
  labels: { app: files-service }
spec:
  selector: { app: files-service }
  ports:
    - { name: http, port: 8193, targetPort: 8193 }

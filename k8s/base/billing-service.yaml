# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  labels: { app: billing-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: billing-service } }
  template:
    metadata:
      labels: { app: billing-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9191"
        prometheus.io/path: /actuator/prometheus
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: billing-service
          image: petclinic-ms/billing-service:latest
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
          ports:
            - { name: http, containerPort: 8191 }
            - { name: mgmt, containerPort: 9191 }
          envFrom:
            - configMapRef: { name: petclinic-common }
          resources:
            requests: { cpu: 200m, memory: 384Mi }
            limits: { cpu: "1", memory: 768Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9191 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9191 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9191 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  labels: { app: billing-service }
spec:
  selector: { app: billing-service }
  ports:
    - { name: http, port: 8191, targetPort: 8191 }
    - { name: mgmt, port: 9191, targetPort: 9191 }

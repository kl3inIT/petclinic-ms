# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: discovery-server
  labels: { app: discovery-server, tier: platform }
spec:
  replicas: 1
  selector: { matchLabels: { app: discovery-server } }
  template:
    metadata:
      labels: { app: discovery-server }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9761"
        prometheus.io/path: /actuator/prometheus
    spec:
      containers:
        - name: discovery-server
          image: petclinic-ms/discovery-server:latest
          ports:
            - { name: http, containerPort: 8761 }
            - { name: mgmt, containerPort: 9761 }
          env:
            - { name: SPRING_PROFILES_ACTIVE, value: "gke" }
            - { name: SPRING_DOCKER_COMPOSE_ENABLED, value: "false" }
            - { name: CONFIG_SERVER_URL, value: "http://config-server:8888" }
            - { name: OTLP_ENDPOINT, value: "http://otel-collector:4318/v1/traces" }
            - { name: JWKS_URI, value: "http://auth-service:8183/.well-known/jwks.json" }
          resources:
            requests: { cpu: 200m, memory: 256Mi }
            limits: { cpu: "1", memory: 512Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9761 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9761 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9761 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: discovery-server
  labels: { app: discovery-server }
spec:
  selector: { app: discovery-server }
  ports:
    - { name: http, port: 8761, targetPort: 8761 }
    - { name: mgmt, port: 9761, targetPort: 9761 }

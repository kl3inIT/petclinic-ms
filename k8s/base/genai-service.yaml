# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: genai-service
  labels: { app: genai-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: genai-service } }
  template:
    metadata:
      labels: { app: genai-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9188"
        prometheus.io/path: /actuator/prometheus
    spec:
      containers:
        - name: genai-service
          image: petclinic-ms/genai-service:latest
          ports:
            - { name: http, containerPort: 8188 }
            - { name: mgmt, containerPort: 9188 }
          envFrom:
            - configMapRef: { name: petclinic-common }
          env:
            - { name: SPRING_DATASOURCE_URL, value: "jdbc:postgresql://postgres:5432/petclinic?currentSchema=genai" }
            - { name: PETCLINIC_LLM_BASE_URL, value: "https://openrouter.ai/api/v1" }
            - { name: PETCLINIC_LLM_CHAT_MODEL, value: "anthropic/claude-3.5-haiku" }
            - { name: PETCLINIC_EMBEDDING_BASE_URL, value: "https://api.openai.com/v1" }
            - { name: PETCLINIC_EMBEDDING_MODEL, value: "text-embedding-3-small" }
            - name: PETCLINIC_LLM_API_KEY
              valueFrom: { secretKeyRef: { name: petclinic-secrets, key: PETCLINIC_LLM_API_KEY } }
            - name: PETCLINIC_EMBEDDING_API_KEY
              valueFrom: { secretKeyRef: { name: petclinic-secrets, key: PETCLINIC_EMBEDDING_API_KEY } }
            - name: PETCLINIC_CRYPTO_KEY
              valueFrom: { secretKeyRef: { name: petclinic-secrets, key: PETCLINIC_CRYPTO_KEY } }
          resources:
            requests: { cpu: 200m, memory: 512Mi }
            limits: { cpu: "1", memory: 1024Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9188 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9188 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9188 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: genai-service
  labels: { app: genai-service }
spec:
  selector: { app: genai-service }
  ports:
    - { name: http, port: 8188, targetPort: 8188 }
    - { name: mgmt, port: 9188, targetPort: 9188 }

# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-service
  labels: { app: workflow-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: workflow-service } }
  template:
    metadata:
      labels: { app: workflow-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9190"
        prometheus.io/path: /actuator/prometheus
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: workflow-service
          image: petclinic-ms/workflow-service:latest
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
          ports:
            - { name: http, containerPort: 8190 }
            - { name: mgmt, containerPort: 9190 }
          envFrom:
            - configMapRef: { name: petclinic-common }
          env:
            - { name: CAMUNDA_CLIENT_ZEEBE_GRPCADDRESS, value: "http://camunda:26500" }
            - { name: CAMUNDA_CLIENT_MODE, value: "selfmanaged" }
          resources:
            requests: { cpu: 200m, memory: 384Mi }
            limits: { cpu: "1", memory: 768Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9190 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9190 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9190 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-service
  labels: { app: workflow-service }
spec:
  selector: { app: workflow-service }
  ports:
    - { name: http, port: 8190, targetPort: 8190 }
    - { name: mgmt, port: 9190, targetPort: 9190 }

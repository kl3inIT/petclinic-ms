# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server
  labels: { app: config-server, tier: platform }
spec:
  replicas: 1
  selector: { matchLabels: { app: config-server } }
  template:
    metadata:
      labels: { app: config-server }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9888"
        prometheus.io/path: /actuator/prometheus
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: config-server
          image: petclinic-ms/config-server:latest
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
          ports:
            - { name: http, containerPort: 8888 }
            - { name: mgmt, containerPort: 9888 }
          env:
            - { name: SPRING_PROFILES_ACTIVE, value: "native,gke" }
            - { name: SPRING_DOCKER_COMPOSE_ENABLED, value: "false" }
            - { name: OTLP_ENDPOINT, value: "http://otel-collector:4318/v1/traces" }
            - { name: JWKS_URI, value: "http://auth-service:8183/.well-known/jwks.json" }
          resources:
            requests: { cpu: 200m, memory: 256Mi }
            limits: { cpu: "1", memory: 512Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9888 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9888 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9888 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: config-server
  labels: { app: config-server }
spec:
  selector: { app: config-server }
  ports:
    - { name: http, port: 8888, targetPort: 8888 }
    - { name: mgmt, port: 9888, targetPort: 9888 }

# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  labels: { app: auth-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: auth-service } }
  template:
    metadata:
      labels: { app: auth-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9183"
        prometheus.io/path: /actuator/prometheus
    spec:
      containers:
        - name: auth-service
          image: petclinic-ms/auth-service:latest
          ports:
            - { name: http, containerPort: 8183 }
            - { name: mgmt, containerPort: 9183 }
          envFrom:
            - configMapRef: { name: petclinic-common }
          env:
            - name: ADMIN_PASSWORD
              valueFrom: { secretKeyRef: { name: petclinic-secrets, key: ADMIN_PASSWORD } }
          resources:
            requests: { cpu: 200m, memory: 384Mi }
            limits: { cpu: "1", memory: 768Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9183 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9183 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9183 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels: { app: auth-service }
spec:
  selector: { app: auth-service }
  ports:
    - { name: http, port: 8183, targetPort: 8183 }
    - { name: mgmt, port: 9183, targetPort: 9183 }

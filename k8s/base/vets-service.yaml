# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vets-service
  labels: { app: vets-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: vets-service } }
  template:
    metadata:
      labels: { app: vets-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9182"
        prometheus.io/path: /actuator/prometheus
    spec:
      containers:
        - name: vets-service
          image: petclinic-ms/vets-service:latest
          ports:
            - { name: http, containerPort: 8182 }
            - { name: mgmt, containerPort: 9182 }
          envFrom:
            - configMapRef: { name: petclinic-common }
          env:
            - { name: MINIO_ENDPOINT, value: "http://minio:9000" }
          resources:
            requests: { cpu: 200m, memory: 384Mi }
            limits: { cpu: "1", memory: 768Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9182 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9182 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9182 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: vets-service
  labels: { app: vets-service }
spec:
  selector: { app: vets-service }
  ports:
    - { name: http, port: 8182, targetPort: 8182 }
    - { name: mgmt, port: 9182, targetPort: 9182 }

# GENERATED by k8s/_generate.py — edit the generator, not this file.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: visits-service
  labels: { app: visits-service, tier: app }
spec:
  replicas: 1
  selector: { matchLabels: { app: visits-service } }
  template:
    metadata:
      labels: { app: visits-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9184"
        prometheus.io/path: /actuator/prometheus
    spec:
      containers:
        - name: visits-service
          image: petclinic-ms/visits-service:latest
          ports:
            - { name: http, containerPort: 8184 }
            - { name: mgmt, containerPort: 9184 }
          envFrom:
            - configMapRef: { name: petclinic-common }
          resources:
            requests: { cpu: 200m, memory: 384Mi }
            limits: { cpu: "1", memory: 768Mi }
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 9184 }
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 9184 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 9184 }
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: visits-service
  labels: { app: visits-service }
spec:
  selector: { app: visits-service }
  ports:
    - { name: http, port: 8184, targetPort: 8184 }
    - { name: mgmt, port: 9184, targetPort: 9184 }

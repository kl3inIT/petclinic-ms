# Gateway base URL — proxied bằng vite.config.ts trong dev (server.proxy)
# Production: trỏ tới ingress public.
VITE_API_BASE_URL=/api

# OpenAPI aggregate spec dùng cho codegen (script fetch-openapi)
OPENAPI_SPEC_URL=http://localhost:8080/v3/api-docs
